# labels/helpers/ — Shared Building Blocks

These modules support the creation of label styles by providing reusable primitives across
four concerns: parameter schemas, geometry shapes, text handling, and geometry composition.

Nothing here writes files or imports from styles/. Helpers exist to be used by styles, not the other way around.
